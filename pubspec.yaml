name: appsupport_launcher_windows
description: "Adds Windows application support to JappeOS."
version: 1.0.4
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  collection: ^1.19.1
  crypto: ^3.0.7
  http: ^1.6.0
  image: ^4.9.1
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
